{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cloudflared-remote.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "cloudflared-remote.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
